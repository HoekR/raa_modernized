services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: raa
      POSTGRES_PASSWORD: raa
      POSTGRES_DB: raa_modernized
    ports:
      - "5432:5432"
    volumes:
      - raa_pg:/var/lib/postgresql/data

volumes:
  raa_pg:
