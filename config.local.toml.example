[database]
url = "postgresql+psycopg://raa:raa@localhost:5432/raa_modernized"

[web]
api_port = 8000

[data]
extab_logical = "raa_extab"

# Editorial layer (Milestone E) — copy to config.local.toml (gitignored)
[editorial]
enabled = true
api_key = "change-me-in-config.local.toml"
editor_id = "editor"
cors_origins = ["http://localhost:5174", "http://127.0.0.1:5174"]
